function output = mapRange( outEnd, outStart, inEnd, inMin, input )

if input > inMax
    input = inMax;
end 

slope = (outMax - outMin) / (inMax - inMin);
output = outMin + slope * (input - inMin);
    
end

